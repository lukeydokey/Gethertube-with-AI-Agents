# Docker Compose Environment Variables
# Copy this file to .env.docker and customize as needed
# Usage: docker compose --env-file .env.docker up -d

# PostgreSQL Configuration
POSTGRES_USER=gethertube
POSTGRES_PASSWORD=gethertube123
POSTGRES_DB=gethertube
POSTGRES_PORT=5432

# Redis Configuration
REDIS_PORT=6379

# pgAdmin Configuration (optional - uncomment pgadmin service in docker-compose.yml)
# PGADMIN_EMAIL=admin@gethertube.local
# PGADMIN_PASSWORD=admin123
# PGADMIN_PORT=5050
